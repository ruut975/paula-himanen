import styles from "./Hero.module.css";
import HeroTitle from '../../UI/HeroTitle'
import Description from '../../UI/TextElement'

const Hero = () => (
  <section className={styles.heroSectionFlex}>
    <HeroTitle></HeroTitle>
    <Description
    
  </section>
);

export default Hero;
